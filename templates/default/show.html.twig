{% extends 'base.html.twig' %}

{% block header %}
     {{ include('partial/header.html.twig') }}
{% endblock %}

{% block body %}
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Email</th>
                <th>Localidad</th>            
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ person.name }}</td>
                <td><a href="{{ path('default_show', {id: id}) }}">{{ person.email }}</a></td>
                <td>{{ person.city }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}

{% block stylesheets %}
<style>
    body {
        background-color: pink;
    }
</style>
{% endblock %}